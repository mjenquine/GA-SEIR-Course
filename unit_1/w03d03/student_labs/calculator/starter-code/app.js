let buttons = []
$(() => {
//=================================================
const calculateTotal = () => {
  buttons.push($(event.currentTarget).text())
}
console.log(buttons);
//=================================================
});
