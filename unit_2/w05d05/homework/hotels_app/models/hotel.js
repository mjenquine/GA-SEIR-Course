const mongoose = require('mongoose')
const Schema = mongoose.Schema

const hotelSchema = new Schema (
  {



  }
  {timestamps: true}
)

const Hotel = mongoose.model('Hotel', tweetSchema)

module.export = Hotel
