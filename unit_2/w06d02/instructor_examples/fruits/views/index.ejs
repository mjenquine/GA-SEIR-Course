<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1>Fruits index page</h1>
        <nav>
            <a href="/fruits/new">Create a New Fruit</a>
        </nav>
        <ul>
          <!-- Fruits is equal to the allFruits that was returned from our database -->
            <% for(let i = 0; i < fruits.length; i++){ %>
                <li>
                  <a href="/fruits/<%=fruits[i].id; %>">
                    The <%=fruits[i].name; %>
                  </a>
                    is  <%=fruits[i].color; %>.
                    <% if(fruits[i].readyToEat === true){ %>
                        It is ready to eat
                    <% } else { %>
                        It is not ready to eat
                    <% } %>
                        <!--  ADD DELETE FORM HERE-->
                        <form action="/fruits/<%=fruits[i].id; %>?_method=DELETE" method="POST">
                          <input type="submit" value="DELETE"/>
                        </form>
                        <a href="/fruits/<%=fruits[i].id; %>/edit">Edit</a>
                </li>
            <% } %>
        </ul>
    </body>
</html>
