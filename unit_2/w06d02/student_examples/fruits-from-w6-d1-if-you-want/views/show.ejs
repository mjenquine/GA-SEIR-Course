<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/main.css">
        <title><%=fruit.name.toUpperCase(); %></title>
    </head>
    <body>
        <h1><%=fruit.name; %> show page</h1>
        The <%=fruit.name; %> is  <%=fruit.color; %>.
        <% if(fruit.readyToEat === true){ %>
            It is ready to eat
        <% } else { %>
            It is not ready to eat
        <% } %>
        <nav>
            <a href="/fruits">Back to Fruits Index</a>
        </nav>
        <script src="/js/show/app.js" charset="utf-8"></script>
    </body>
</html>
